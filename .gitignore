# AI working notes (keep locally, not in public repo)
CLAUDE.md

# Environment
.env

# Python
__pycache__/
*.py[cod]
*.pyo
.Python
*.egg-info/
dist/
build/
.venv/
venv/
env/

# Data (raw downloads are large; keep processed outputs)
data/raw/
data/cache/
# DuckDB is committed (small, ~1MB) so GitHub Actions can run score.py without
# re-running the full data collection pipeline.
# Exclude the scratch metrics.duckdb created during development.
data/metrics.duckdb

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Node
node_modules/

# Local dashboard copy (canonical site lives in docs/ for GitHub Pages)
dashboard/
